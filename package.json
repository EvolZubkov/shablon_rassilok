{
  "name": "email-builder",
  "version": "2.2.1",
  "private": true,
  "scripts": {
    "test": "vitest run tests/app.test.js",
    "test:sanitizer": "vitest run tests/textSanitizer.test.js",
    "test:generator": "vitest run tests/emailGenerator.test.js",
    "test:bulk": "vitest run tests/bulkMailPanel.test.js",
    "test:all": "vitest run tests/",
    "test:watch": "vitest tests/",
    "test:ui": "vitest --ui tests/"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.3",
    "jest": "^29.0.0",
    "jsdom": "^24.1.3",
    "vitest": "^4.1.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
